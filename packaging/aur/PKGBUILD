# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-solana-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-solana-recorder (CodeTracer recorder for Solana programs (Rust/SBF)).

pkgname=codetracer-solana-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Solana programs (Rust/SBF)"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-solana-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-solana-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-solana-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-solana-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-solana-recorder
}

check() {
  cd "$srcdir/codetracer-solana-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-solana-recorder || true
}

package() {
  cd "$srcdir/codetracer-solana-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-solana-recorder" \
    "$pkgdir/usr/bin/codetracer-solana-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
